{
  "id": "claude-code-ephemeral",
  "version": "1.0.0",
  "name": "Claude Code (Ephemeral)",
  "description": "Installs Claude Code with config and credentials kept on the container's in-memory /dev/shm tmpfs. Nothing is written to disk, and everything (including your login) is wiped when the container stops, forcing a fresh login next start. Intended for shared machines where a persistent credential volume would be a theft risk.",
  "customizations": {
    "vscode": {
      "extensions": ["anthropic.claude-code"]
    }
  },
  "containerEnv": {
    "CLAUDE_CONFIG_DIR": "/dev/shm/claude-config"
  },
  "postStartCommand": "mkdir -p /dev/shm/claude-config && chmod 700 /dev/shm/claude-config",
  "dependsOn": {
    "ghcr.io/devcontainers/features/node:1": {}
  }
}
